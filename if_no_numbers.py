#!/usr/bin/env python3
""" si el usuario no tiene número asociado:
    - escribir json con el número telefónico
    - asociado al número de usuario
"""

""" pasos:
    - leer json "url_user.json"
    - ver si no tiene número asociado en "user_id_num.json"
    - si no tiene:
    - llamar a la función obtener_numero_selenium()
    - insertar número telefónico a json "user_id_num.json"
"""

def if_no_numbers():
    pass


if __name__ == "__main__":
    if_no_numbers()
